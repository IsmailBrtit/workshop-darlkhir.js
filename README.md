# Chrono

This was my **first web project**, made with basic **HTML**, **CSS**, and **JavaScript**.

I built it to try out buttons, styling, and simple DOM manipulation.  
It might not be perfect — but it’s where everything started.
